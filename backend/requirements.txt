# =============================================================================
# OpenQwnt Trading Platform - Backend Dependencies
# =============================================================================

# Core Framework
fastapi
uvicorn[standard]
python-dotenv
httpx[http2]
h2
pydantic

# Data Processing
pandas
numpy

# Trading & Backtesting
nautilus_trader
backtesting
backtrader
trading-ig
python-binance
TA-Lib

# Database
sqlalchemy
aiosqlite

# Async HTTP
aiohttp

# Market Data
yfinance
# curl_cffi provides a chrome-impersonating Session that dodges the
# YFRateLimitError storm Yahoo introduced in 2025-2026. Optional — the
# terminal_data router checks for it at runtime and falls back cleanly.
curl_cffi

# News Feeds
feedparser

# Avanza integration (TOTP login + encrypted credential storage + push WS)
pyotp
cryptography
websockets

# Quantitative Analytics
quantstats
statsmodels

# RAG & Vector DB
chromadb
sentence-transformers

# AI/LLM - Google ADK for agents
google-adk
google-generativeai
# Live API (bidirectional speech-to-speech) — used by services/voice
google-genai

# AI/LLM - Optional providers
openai
anthropic

# Caching
redis

# Testing
pytest
pytest-asyncio

# Utilities
pytz
python-multipart

# Voice subsystem
twilio                # Programmable Voice + SMS (already used by orchestrator)
aioapns               # APNs HTTP/2 client for VoIP push to the iOS app
phonenumbers          # E.164 validation for the profile phone field
